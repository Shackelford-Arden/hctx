## v1.4.1
### Fixed
* Bug keeping stack tokens from properly being cached when switching stacks (or unsetting)
* Fixing some inconsistency around shell discovery
### Internal
* Moving to golangci-lint for linting and `mise` for tooling
